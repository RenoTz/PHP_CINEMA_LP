<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <title>Cinéma</title>
        <link rel="stylesheet" type="text/css" href="{{ app.request.basePath }}/css/style.css" />
        <link rel="shortcut icon" href="{{ app.request.basePath }}/css/favicon.ico" />
    </head>
    <body>
        <h1>Cinéma</h1>
        <div class="menu">
            <a href="{{ path('home') }}">Accueil</a>
            <a href="{{ path('films') }}">Films</a>
            <a href="{{ path('genres') }}">Genres</a>
            <a href="{{ path('meilleurs_films') }}" style="width:130px;">Meilleurs films</a>
            <a href="{{ path('ajout_film') }}" style="width:130px;">Ajouter un film</a>
            <a href="{{ path('suppression_film') }}" style="width:160px;">Supprimer un film</a>
        </div>        

        <div class="contents">
            {% block content %}{% endblock %}
        </div>
    </body>
</html>
